#include <vector>
#include "Parser.hpp"
#include "Planner.hpp"
//---------------------------------------------------------------------------
using namespace std;
//---------------------------------------------------------------------------
Plan& Planner::generatePlan(QueryInfo &query)
{
    Plan *p = new Plan();

    return *p;
}
//---------------------------------------------------------------------------
Plan& Planner::generateAllPlans(vector<QueryInfo> queries)
{
    Plan *p = new Plan();

    return *p;
}
//---------------------------------------------------------------------------
